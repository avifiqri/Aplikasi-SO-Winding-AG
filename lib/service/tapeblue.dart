import 'package:flutter/material.dart';

class Tapeblue extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Hitung SO Tape Blue"),
      ),
      body: Container(
        margin: EdgeInsets.all(20),
        padding: EdgeInsets.all(10.0),

        child: Center(

          child: Text(
            "hellow Tape Blue"
          ),
        ),
      ),
    );
  }
}
